import React from "react";
import MenuRoutes from './menu-routes';
import BuildItem from "../build-item";

const Breadsticks = () => (
  <MenuRoutes >
    <BuildItem category="breadsticks" />
  </MenuRoutes>
);

export default Breadsticks;
